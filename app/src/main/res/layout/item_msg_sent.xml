<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:id="@+id/ll_msg_send"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="5dp"
        android:background="@drawable/msg_sent_drawable"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/iv_msg_send"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:paddingStart="5dp"
            android:paddingTop="5dp"
            android:paddingEnd="5dp"
            android:paddingBottom="5dp"
            android:visibility="gone"
            android:src="@drawable/place_holder"
            android:scaleType="centerCrop"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/tv_msg_send"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="10dp"
            android:paddingTop="5dp"
            android:paddingEnd="10dp"
            android:paddingBottom="5dp"
            android:textSize="18sp"
            android:visibility="gone"
            tools:text="TextView"
            tools:visibility="visible" />
    </LinearLayout>


    <ImageView
        android:id="@+id/iv_react"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginStart="5dp"
        android:layout_marginTop="-5dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="@+id/ll_msg_send"
        app:layout_constraintTop_toBottomOf="@+id/ll_msg_send"
        app:srcCompat="@drawable/ic_fb_love"
        tools:visibility="visible" />
</androidx.constraintlayout.widget.ConstraintLayout>